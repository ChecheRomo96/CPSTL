#include <CPiostream.h>

namespace cpstd{

    


    ostream cerr(nullptr);
    ostream clog(nullptr);

    wistream wcin(nullptr);
    wostream wcout(nullptr);
    wostream wcerr(nullptr);
    wostream wclog(nullptr);
}